# Rust 格式化配置（仅 stable 特性，避免 nightly-only 警告）。
# Rust formatting configuration using stable features only to avoid nightly-only warnings.
edition = "2021"
max_width = 100
use_field_init_shorthand = true
use_try_shorthand = true
